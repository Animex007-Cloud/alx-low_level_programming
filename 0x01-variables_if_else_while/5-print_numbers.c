#include <stdio.h>
/**
 * main - Always entry
 * Return: 0 (Successful)
 */
int main(void)
{
	char base = '0'

	for (base = '0'; base <= '9'; base++)
	{
	putchar(base);
	}
	putchar('\n')
	return (0);
}
