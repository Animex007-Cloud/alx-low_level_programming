#include <stdio.h>
/**
 * main - Always entry
 * Return: 0 (successful)
 */
int main(void)
{
	int cha = 0;
	int hexdigit = 'a';
	
	while (cha = 0; cha <= 9; cha++)
	{
	putchar(cha);
	}
	while (hexdigit = 'a'; hexdigit <= 'f'; hexdigit++)
	{
	putchar(hexdigit);
	}
	putchar('\n');
	return (0);
}
