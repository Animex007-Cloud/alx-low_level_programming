#include <stdio.h>
/**
 * main - Always entry
 * Return: 0 (successful)
 */
int main(void)
{
	int cha = 0;
	int hexdigit = 'a';
	
	for (cha = 0; cha <= 9; cha++)
	{
	putchar(cha);
	}
	else
	{
       	(hexdigit <= 'f');
	}
	putchar(hexdigit);

	putchar('\n');
	return (0);
}
