#include <stdio.h>
/**
 * main - Always entry
 * Return: 0 (Successful)
 */
int main(void)
{
	char = num;

	for (num = 0; num < 10; num++)
	{
	putchar(num % 10) + '0');
	}
	putchar('\n');
	return (0);
}
